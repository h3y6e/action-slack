name: post-release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

permissions:
  contents: write

jobs:
  update-semver:
    runs-on: ubuntu-slim
    timeout-minutes: 5

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: haya14busa/action-update-semver@7d2c558640ea49e798d46539536190aff8c18715 # v1.5.1
        with:
          major_version_tag_only: true

  notify:
    needs: update-semver
    runs-on: ubuntu-slim
    timeout-minutes: 5

    steps:
      - uses: h3y6e/action-slack@v4
        with:
          status: ${{ needs.update-semver.result }}
          text: 'Released ${{ github.ref_name }}'
          fields: repo,commit
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
